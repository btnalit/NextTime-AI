{
  "name": "nexttime-ai",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "depcruise": "depcruise --config .dependency-cruiser.cjs --output-type err packages/*/src"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.5",
    "dependency-cruiser": "^16.10.0",
    "typescript": "^5.8.2",
    "vitest": "^2.1.8",
    "yaml": "^2.7.0"
  }
}
