# 控制台验收审计（2026-09-23，公开版）

> 来源：2026-09-23 对主机 v0.16.2 控制台的逐页审计（27 个视图 × 1440 / 1280 / 768 + 关键状态）、入口 agent 自测核验、"让入口 agent 能执行"任务流走查与经维护者同意的一次真跑。带截图、真实名称与 id 的完整版在 gitignored 的 `docs/private/ui-audit-2026-09-23/`；本文件是去掉工作区名、门 / 主体 / 对话 id 之后的问题清单，供 S8 各车道引用 （`development-tasks.md` §5e）。评分标尺是 `console-completion-plan.md` §5.9 的六条原则 + WCAG 2.2 AA + UX 文案。

> 截图方式：1440 / 1280 / 768 是页内仿真（body 固定目标宽度、改写 `@media`、vh / vw 换像素）；走查与真跑是真实窗口。标"需真窗复核"的布局项实施前先在真实窄窗口确认。级别：P0 阻断核心用途或误导决策；P1 严重；P2 明显但局部；P3 打磨。波次见 `development-tasks.md` §5e。

共 98 条：P0 4、P1 28、P2 47、P3 19。


## 入口 agent 自测核验（B）

| ID | 位置 | 级别 | 类别 | 元素 | 问题 | 修法 | 波次 |
|---|---|---|---|---|---|---|---|
| B1 | 入口 agent 自测核验 | P0 | 数据完整性 | host-inventory 采集器 → 一个 accept-s3 临时验收工作区 | 生产采集器自 09-18 09:59 起写进临时验收工作区（194,620 条观测）；生产默认工作区 图谱停在 09-18 09:50，agent 按旧观测回答。该工作区 09-25 09:59 UTC 到期。 | 到期前把采集器 Handle 重铸回 生产默认工作区（主机操作，需你决定）；给 collector_silent 在控制台概览加“图谱新鲜度”指标；验收脚本改用私有 token 文件并加守卫：检测到生产 token 被改写即失败。；参照 遗留 41 / 62 | W0 |
| B2 | 入口 agent 自测核验 | P1 | prompt 契约 | ontology/entry-agent.yaml:62-133 | 无“find_* 全空”指引；承诺的 ops-runner 在生产工作区不存在；无回复语言指令。agent 因此自拟了“给我的 Handle 授 scope（或补 WorkerDefinition）”。 | 补一段：全空 = 本工作区还没有已发布的 Worker / 授权的门，属于 owner 操作（目录发布 Worker + 系统接入授权门给成员，两者都要）；ops-runner 改成“若目录里有”；加语言指令；prompt-contract 守卫加对应断言。；参照 §A5 能力模型 | W2 |
| B3 | 入口 agent 自测核验 | P1 | 能力设计 | find-means.ts:98-130 | find_* 用 need 做子串 ILIKE，自然语言需求几乎不可能命中；12 个 Operation 描述为空，连关键词也只能撞名称。 | 短期：工具描述写明“need 是关键词、空串列出全部”；导入 manifest 时强制非空描述。中期：分词 / 全文检索（pg tsvector）或让 find_* 返回全部候选由模型挑。 | W2 |
| B4 | 入口 agent 自测核验 | P2 | 一致性 | service.ts:511-571 / handles.ts:193-201 | find_procedures 的步骤可用性判断不套 enabledWorkerDefinitions（invoke_worker 会拒）；handles.ts 注释仍称 connect_gatekeeper 未实现。 | 步骤判断复用 find_workers 的白名单过滤；删掉过时注释。 | W2 |
| B5 | 入口 agent 自测核验 | P2 | 验收脚本 | scripts/accept_s2.sh:119-121 vs 1010-1027 | fixture 清理不在 EXIT trap 里，中途失败必留 accept-s2-* 容器。 | 把 compose --profile accept-s2 rm -sf 并入 trap（幂等），与 accept_provider_restore 同路径执行。 | W4 |
| B6 | 入口 agent 自测核验 | P2 | 残留 | 工作区 / 入口容器 | 14 个 accept-* 工作区仍活跃；28 个已退出的 nexttime-entry-* 容器，约 7 个属已清除工作区（孤儿）。 | 临时工作区等到期清理；常规的 一个 accept-s1 常规验收工作区 控制台停用；孤儿入口容器需要 supervisor 侧回收（清除工作区时一并删容器）。 | W4 |
| B7 | 入口 agent 自测核验 | P2 | 治理语义 | 生产默认工作区 的门 | 门走旧 register-gatekeeper 路径：Operation 已发布、但 0 条 workspace_gate_link、0 条授权；两套机制在系统接入页并存且无解释。 | 定一个权威路径（门实例启用），给旧注册写迁移或在页面标“旧注册，建议迁移”；页面说明 agent 能用门的完整前提。 | W2 |
| B8 | 入口 agent 自测核验 | P3 | 模型 | deepseek-v4-flash | 回复里混入俄语词。已证实：英文 prompt、无语言指令、无采样覆盖；原因属推断。 | 随 B2 加语言指令；如仍出现，入口默认模型换 v4-pro 做对照。 | W2 |

## 任务流走查：让入口 agent 能执行（J）

| ID | 位置 | 级别 | 类别 | 元素 | 问题 | 修法 | 波次 |
|---|---|---|---|---|---|---|---|
| J1 | 任务流走查 | P0 | 执行闭环 | 平台概览 · 开始使用 / 系统接入 / 目录 / 访问 | 让 agent 能执行需要「启用门 + 授权门给成员 + 发布 kind=worker 定义」三者同时成立，没有任何一页把这条链讲全；平台概览「开始使用」5 项全绿，却没有一项检查执行就绪。 | 概览加「执行就绪」检查（每个成员：可用门数 / 已授权门数 / 可委派 Worker 数，缺哪项链到哪页）；系统接入、目录、访问三页顶部复用同一个「执行前提」提示条。；参照 §5.9 控制塔 | W2 |
| J2 | 任务流走查 | P1 | 流程 | 启动器第 3–4 步 | 「在本工作区启用」是第 3 步的可选按钮，不点也能下一步；第 4 步显示「本工作区：未启用」却提示「该门实例可用」，并给出醒目的「完成」——不启用也能完成。授权成员的步骤只在启用后才出现。 | 第 3 步把启用做成必经步骤（或第 4 步在未启用时禁用「完成」并说明）；授权成员固定为第 3 步的一部分；完成页列出「已做 / 未做」。；参照 §5.9 Launcher | W2 |
| J3 | 任务流走查 | P1 | 确认分级 | 系统接入 · 在本工作区启用 | 一键写入：注册 Gatekeeper、导入并发布它 announce 的全部 Operation（含 execute / 高影响），没有预览或确认。 | 中等级 ConfirmTier：列出将发布的 Operation 与影响级，确认后执行。；参照 §5.9 原则 4 | W2 |
| J4 | 任务流走查 | P1 | 数据完整性 | 启动器 · 已有旧注册 | 生产默认工作区 已有旧路径注册的 docker 门（旧 docker 门），启动器不识别，仍提供「在本工作区启用」——会再注册一个 Gatekeeper、再发布一套重复 Operation——代码已确认：enable_gate_instance 只按门关联判重，registerGatekeeperObject 不带身份键新建对象，且内核没有撤销 Gatekeeper 的能力。真跑时因此没有点击。 | 启用前按 target 检测已有 Gatekeeper，提供「关联 / 迁移旧注册」而不是新建；修之前给旧注册写迁移（把已有 Gatekeeper 关联到门实例）。；参照 B7 | W2 |
| J5 | 任务流走查 | P2 | 引导 | 启动器第 1–2 步 | 第 1 步只有 HTTP / MCP / SSH / CLI，没有「使用已接入的系统」；docker 该选哪种要靠猜（接入包写 http，卡片写 cli）。第 2 步默认展开管理员专用的新建表单，「选择已有实例」在折叠线下；选类型后的说明带内部代号 P-B2a 并把「下一步」挤出窗口（抽屉底栏不固定）；实例列表的「已启用」指平台级。 | 第 1 步先列「平台已提供的系统」；非管理员隐藏新建表单；抽屉底栏 sticky；状态拆成「平台 / 本工作区」两个 chip。；参照 §5.9 Launcher | W2 |
| J6 | 任务流走查 | P1 | 流程 | 访问 · 授予能力 / 系统卡片 · Grant to principal | 两个授权入口各缺一半：访问页能选成员、门要手填 id，留空 = 授予全部门且无提示，资源类型是自由文本，范围要写 JSON，副标题是 API 签名；系统卡片门固定、成员要粘 UUID。两处互不提及。 | 合成一个授权流程：选成员 + 选门（可多选）+ 可选收窄到部分 Operation；「全部门」必须显式选择并走确认；两处入口打开同一个抽屉。；参照 §5.9 原则 3 / 4 | W2 |
| J7 | 任务流走查 | P1 | 表单 | 目录 · 新建 Worker 定义 | 能力、可作用的门、Skill、出网拒绝全是无提示的空白文本框；模型要手写 provider/model；没有 ops-runner 模板；「类型」允许再建 entry；说明里带内部编号 I16。owner 实际上无法从控制台写出一个可用的 Worker。 | 能力 / 门 / Skill 用目录选择器；模型用下拉；提供「从模板创建（ops-runner）」；owner 视角隐藏 kind=entry；保存与发布是清楚的两步。；参照 CW1 | W2 |
| J8 | 任务流走查 | P2 | 可追溯 | 对话 · 助手消息 | agent 引用的对象、边和操作名（如 ComposeProject、某条边的 id、find_workers）都不可点，核实只能手抄去图谱搜索或审计的裸 id 输入框。 | 对已知实体做行内链接，直达图谱对象页或审计 explain；与 C2 的 Markdown 渲染一起做。；参照 §5.9 ProvenanceChain | W3 |

## 真跑验证（R）

| ID | 位置 | 级别 | 类别 | 元素 | 问题 | 修法 | 波次 |
|---|---|---|---|---|---|---|---|
| R1 | 真跑验证 | P1 | 结果交付 | Worker 结果契约 · artifacts | Worker 把完整清单写进它自己容器里的 /workspace/docker-container-inventory.md，并作为 artifact 上报；容器结束后路径不复存在，入口 agent 的 read 失败，用户在任务页也打不开。 | report_result 的 artifacts 由平台落盘（对象存储 / 审计附件）并给出可下载链接；或限制为内联内容。 | W4 |
| R2 | 真跑验证 | P2 | 成本 | ops-runner · 单次只读盘点 | 一次 containers.list 让 Worker 用掉 118,345 token（单任务预算的 59%），整轮 10 次调用约 $0.087；主要是长 system prompt、工具清单和 66 个容器的原始输出。 | 门操作返回做截断 / 摘要；Worker 工具清单只下发本任务的门；预算接近时提前告警。 | W4 |
| R3 | 真跑验证 | P2 | 数据回写 | Worker 结果 · factsToAssert | Worker 做了一次实时观测却 factsToAssert=0，图谱没有因此刷新——生产默认工作区 的图仍停在 09-18（B1）。 | 观测类门操作的结果由平台直接作为 Observation 写回（不依赖 Worker 自愿断言），或在 ops-runner prompt 里要求把盘点结果写成 Facts。；参照 B1 | W4 |
| R4 | 真跑验证 | P2 | 工具契约 | invoke_worker · gates 参数 | agent 第一次把门名 docker 当作 gates 传入被拒，第二次改用 id 才成功；find_operations 返回里 id 不够显眼。 | invoke_worker 接受门名或在报错里列出可用 id；工具描述写明 gates 是 Gatekeeper id。 | W2 |
| R5 | 真跑验证 | P2 | 反馈 | 系统卡片 · Grant to principal | 授权成功只有一个 Toast，卡片上看不到「谁已获授权」，刷新后无从确认。 | 卡片显示已授权成员列表与撤销入口。；参照 U2 / J6 | W2 |
| R6 | 真跑验证 | P2 | 流程陷阱 | Worker 编辑器 · 完成 vs 发布 | 保存草稿后给出「发布」和「完成」两个按钮；点「完成」的话草稿不会出现在任何列表里（Workers 只列已发布），等于找不回来。 | 列表加「我的草稿」；或保存后默认聚焦「发布」并提示未发布的后果。；参照 J7 | W2 |
| R7 | 真跑验证 | P3 | 呈现 | 任务列表行 | 行副标题直接显示原始 JSON 输入（{“intent”:“Inventory the Docker containers…）。 | 显示 intent 文本，JSON 放详情。 | W3 |
| R8 | 真跑验证 | P3 | 残留 | nexttime-ai-fake-llm-1 | 验收用的 fake-llm 容器仍在运行（约 4 小时），真实 llm-proxy 与之并存。 | 验收脚本结束时停掉 fake-llm（并入 B5 的 trap）。；参照 B6 | W0 |
| R9 | 真跑验证 | P3 | 语言 | 入口 / Worker 回复 | 本次入口 agent 的中间消息全是英文、最终答复才用中文，Worker 摘要是英文——再次印证缺回复语言指令。 | 见 B2。；参照 B2 | W2 |

## 系统性问题（S / U / L，跨页）

| ID | 位置 | 级别 | 类别 | 元素 | 问题 | 修法 | 波次 |
|---|---|---|---|---|---|---|---|
| S1 | 跨页（系统性） | P0 | 布局 | PageHeader .page-header-actions | 操作区不换行，挤压标题列：系统接入 1280 标题折成“系统接 / 入”，768 一字一行、按钮溢出到 x=1001；成员 / 用户 / 模型页 768 同样被挤窄。（需真窗复核） | 操作区 flex-wrap；标题列设最小宽度；≤1100 操作整体换到标题下方。（1440 档已在真实窗口 1422 确认；1280 / 768 仍待真窗复核）；参照 §5.9 壳与导航：统一页头 | W1 |
| S2 | 跨页（系统性） | P1 | 壳 / 响应式 | 侧栏 ≤960 | 侧栏收成 21 个无文字图标：分组标题、工作区 + 角色、当前用户全部消失；图标重复（⊞ = 目录 / 工作区 / 模块 / 平台设置；芯片 = 模型与配额 / 模型与供应商 / 运行层；人像 ×2；放大镜 ×2）。（需真窗复核） | ≤960 改为可展开的抽屉式侧栏或带文字顶栏；图标去重；工作区与身份常驻。；参照 §5.9 壳与导航 | W1 |
| S3 | 跨页（系统性） | P1 | 响应式 | 所有数据表 @768 | 每张表都只能横向滚动（用户 907/656、工作区 1001/656、供应商 1543、运行层 809、配额 808），状态与操作列在屏外；1440 下供应商表也溢出（1455/1120）。 | 窄屏卡片化，或把名称 + 状态 + 操作列 sticky；先砍掉双语表头的一半宽度。 | W1 |
| S4 | 跨页（系统性） | P1 | 文案 / i18n | 全站标签 | “中文 English”并排使所有文案长度翻倍，是 1280 / 768 折行的主因；同时一批只有英文（Connection requests、Registered systems、Grant 表单、我的智能体 / 平台设置 / 模型页副标题、平台审计筛选、BLAST RADIUS）。 | 定中文为界面语言，英文放 title / 语言切换；补齐漏译。；参照 §5.9 文字节奏 | W1 |
| S5 | 跨页（系统性） | P2 | 无障碍 · 对比度 | --text-3 × --bg / --accent-soft | #6b7280 在 #f4f5f7 上 4.43:1、在选中底 #e8eefc 上 4.16:1，低于 4.5:1（页面副标题、侧栏选中项副标签、表头）。§5.9 只承诺了对白底 4.7:1。 | --text-3 调深到约 #5f6672，或副标题改用 --text-2。；参照 §5.9 原则 6 | W1 |
| S6 | 跨页（系统性） | P2 | 无障碍 · 点击区 | 复制按钮、面包屑、前往 Go | 20×20 的复制按钮（图谱 51 个、审计 58–66 个）、16px 高的面包屑、45×21 的“前往 Go”。 | 命中区至少 36px（触屏 44px），视觉尺寸可保持。；参照 §5.9 原则 6 | W1 |
| S7 | 跨页（系统性） | P2 | 无障碍 · 语言 | <html lang> | 文档声明 lang=“en”，界面主体是中文：读屏用英文发音，CJK 断行规则不对。 | lang=“zh-CN”，英文片段标 lang=“en”。；参照 WCAG 3.1.1 | W1 |
| S8 | 跨页（系统性） | P2 | 无障碍 · 字号 | 11px 文本 | 侧栏副标签、chip、图谱与审计里大量 11px 文本（图谱 112 处、审计 87 处）。 | 最小 12px（--fs-12）。；参照 §5.9 字号刻度 | W1 |
| S9 | 跨页（系统性） | P1 | 数据呈现 | 时间格式化 | en-US 格式、不带时区（浏览器在 PDT，主机在 CST）；相对时间把未来时间显示成“just now”（工作区“到期 expires just now”，实际 09-25 / 09-30）；“just now”与“0m ago”混用。 | 一个 formatter：未来时间“N 天后到期”，绝对时间带时区，按界面语言本地化。 | W1 |
| S10 | 跨页（系统性） | P1 | id 暴露 | 多页 | §5.9 原则 3 被多处违反：默认工作区是 UUID 文本框；常驻容器列显示已清除工作区的 UUID；门实例 Gate id 截成“gatekeep”；Worker 定义只显示 入口定义；授权表单要粘贴 principal UUID；审计 explain / reconstruct 只收裸 id。 | 统一用 RefChip（名称 + 类型 + 截断 id + 复制）和选择器，裸 id 只作兜底。；参照 §5.9 原则 3 | W1 |
| S11 | 跨页（系统性） | P1 | 审计 | 审计流 / 平台审计 / 概览 | 控制台每打开一页产生约 8 条 list_* 读审计；概览与运行状态的“最近平台审计”全是读；每次打开模型页都签发一次 llm admin token 并记两条审计。 | 审计流默认只显示写与决策，读另设筛选；admin token 按会话缓存。；参照 §5.9 审计：结构化审计流 | W3 |
| S12 | 跨页（系统性） | P2 | 页头 / 面包屑 | 多页 | 对话、概览、我的、成员、访问、模型、用户、工作区、设置、平台审计没有面包屑；同一分组三种叫法：工作 / 使用 Work / USE，治理 Govern / Governance / GOVERN。 | 所有页走同一个 PageHeader；分组名统一。；参照 §5.9 壳与导航 | W1 |
| S13 | 跨页（系统性） | P1 | 确认分级 | 集成模式、弃用、回滚 | 集成页“模式”是行内下拉、改完立即生效无确认（可把接入包全平台 disabled）；目录“弃用”是普通文字按钮；中等级确认渲染在页面最底部（回滚按钮在页顶，确认出现在约 2000px 以下）。 | ConfirmTier 就近弹出；模式改成抽屉 + 分级确认；弃用走中 / 高级确认。；参照 §5.9 原则 4 | W1 |
| S14 | 跨页（系统性） | P2 | 文案 | 内部术语外泄 | P-C、遗留 6、内部指标名、allowMemberAutoApproveLow、NEXTTIME_PLATFORM_ADMINS、PI_DRIFT_FILE 路径、“see docs/runbooks”、bootstrap add-principal、llm-proxy、原始枚举 platform_preset / self_serve / owner / entry、“(s)”复数。 | 改成用户能认的说法；枚举走 StatusChip 映射。；参照 §5.9 原则 2 | W1 |
| S15 | 跨页（系统性） | P1 | 身份 | 侧栏 / 成员 / 审计 / 工作区 | 同一个人三个名字：用户 admin、显示名 Administrator、工作区主体 owner。审计记 owner，侧栏显示 Administrator，工作区 owners 显示 admin，成员页不标“你”。 | 成员与审计里给当前用户标“你”；统一显示名来源；我的账户里写明“在 生产默认工作区 中你是 owner”。 | W3 |
| U1 | 跨页（系统性） | P2 | 信息架构 | 侧栏 · 治理 / 平台 | 两组的作用范围（本工作区 / 全平台）从未说明，系统接入 vs 集成、模型与配额 vs 模型与供应商、审计 vs 平台审计看起来像同义重复。 | 分组标题下各加一行范围说明，每页 PageHeader 副标题固定带范围；长期按 platform-admin-design 的「使用面 / 管理面 / 维护面」收敛。；参照 §5.9 壳与导航 | W3 |
| L2 | 跨页（系统性） | P1 | 视觉层级 | 主按钮数量 | 多处同一视图两个 ink 主按钮：用户页「新建用户」+「清理待激活用户」，集成门实例标签「接入一个系统」+「新建门宿主实例」，我的账户三个表单各一个。 | 每个视图只留一个 ink 主按钮，其余用描边；批量清理走 ConfirmTier。；参照 §5.9 原则 1 | W1 |
| L3 | 跨页（系统性） | P1 | 语义色 | 影响级展示 | 治理影响级（execute、high / medium、auto-approvable）在系统卡片和授权表单里只是裸色文字，而次要的对象状态都是 dot + pill——最重要的信息反而最轻。 | Operation 的模式与影响级统一走 StatusChip，与目录页一致。；参照 §5.9 原则 2 | W1 |
| L5 | 跨页（系统性） | P2 | 一致性 | 分区外壳 | 同类页面用了三种分区方式：直接铺在背景上（模型与配额、访问）、每段一张卡（概览）、一张卡加分割线（平台设置）。 | 表单页统一「一张卡 + 分割线」，仪表盘页统一「独立卡片」。；参照 §5.9 原则 5 | W1 |
| L8 | 跨页（系统性） | P2 | 一致性 | 抽屉结构 | 抽屉里只读元数据、说明加链接、可编辑表单三种内容贴在一起，只靠细线分隔；段标题大小写不一（WORKSPACES USING IT vs Display name）。 | 抽屉固定三段：元数据 / 相关链接 / 编辑表单，统一段标题样式。 | W1 |
| L11 | 跨页（系统性） | P3 | 密度 | 短列表页 | 对话、成员、模块、Workers 只有 1–2 行时，下方 70–90% 是空背景，看起来像没做完。 | 短列表不撑满；空余处给下一步提示（例如 Workers 的「新建 / 从模板创建」）。 | W3 |

## 逐页问题

| ID | 位置 | 级别 | 类别 | 元素 | 问题 | 修法 | 波次 |
|---|---|---|---|---|---|---|---|
| C1 | 对话列表 | P2 | 数据呈现 | 对话行副标题 | 显示的是 chats.created_at（09-18）→“4d ago”，而这条对话今天刚有一轮。 | 显示最近活动时间 + 状态副标题（基线：列表带状态副标题）。；参照 §5.9 页面对照：对话 | W3 |
| C2 | 对话详情 | P0 | 可读性 | 助手消息 | 助手消息不渲染 Markdown：##、**、表格竖线、反引号原样显示，自测报告几乎不可读。 | 消息用受限 Markdown 渲染（标题、粗体、表格、代码、列表），不开 HTML。 | W1 |
| C3 | 对话详情 | P1 | 布局 | 对话顶栏 | 1280 模型下拉折到第二行、行尾孤立“·”；768 改名 / 归档与模型下拉重叠，“模型 Model:”折成两行。（需真窗复核） | 顶栏两行固定结构：标题行 + 模式 / 模型行，操作收进溢出菜单。；参照 §5.9 页面对照：对话 | W1 |
| C4 | 对话详情 | P3 | 文案 | 消息角色 | 角色标签原样显示 user / assistant。 | 显示“你” / 入口 agent 名称。 | W3 |
| G1 | 图谱 | P1 | 新鲜度 | 整张图 | 所有对象“陈旧 Aging · 5d ago”（窗口 2h）：超出窗口 5 天仍只是“陈旧”，也没有“整张图停更”的警示——正是这个让 agent 与人都没察觉 B1。 | 超窗 N 倍标“过期”；页顶显示最后一次采集时间与来源工作区；接概览“图谱新鲜度”指标。；参照 §5.9 控制塔：图谱新鲜度 | W3 |
| G2 | 图谱 | P2 | 布局 | 列表 / 详情分栏 | 1440 下左列表只有约 285px、右侧大面积空白；sha256 名称截断；默认“最近更新”被镜像 sha256 占满。 | 列表加宽或可拖拽；默认排除 Image 或按类型分组。 | W3 |
| G3 | 图谱 | P3 | 表单 | 查询区 | 查询 / 类型表单上下错位；“window 2 h”折行；“截至 As of”用 en-US 原生日期控件（mm/dd/yyyy）。 | 统一表单栅格；日期控件本地化。 | W3 |
| M1 | 我的智能体 | P2 | 语义 | 当前生效 · Worker 定义 | 把入口定义当 Worker 展示（“Worker 入口定义”），只有 id 没有名称——entry 与 worker 语义漂移。 | 标成“入口定义”并显示名称与版本。；参照 §5.9 原则 3 | W3 |
| M2 | 我的智能体 | P3 | 文案 | 副标题 / 帮助 | 副标题与帮助文案只有英文，带 llm-proxy 术语；暴露 allowMemberAutoApproveLow；长单列表单无分节。 | 分“模型 / 能力 / 提示词 / 自动批准”四节；文案本地化。 | W3 |
| AC1 | 我的账户 | P3 | 细节 | 我的工作区 | 工作区行有无意义的左缩进；角色 chip 原样 owner；“绑定已有 API key”与概览页重复。 | 去掉缩进；角色走 StatusChip；API key 只留在我的账户。 | W3 |
| AX1 | 访问 | P3 | 表单 | 签发外部运行时凭证 | 能力勾选是页内嵌套滚动框；“能力 Capabilities *”标签与星号分离；主体筛选用等宽占位符。 | 能力按分组折叠而非内嵌滚动；标签对齐。 | W2 |
| SY1 | 系统接入 | P1 | 语义 | 从平台目录启用 / Registered systems | 同一个门在“从平台目录启用”里是“在本工作区启用”（未启用），在“Registered systems”里却已发布 5 / 7 个 Operation——两套接入机制并存，页面不说哪一套决定 agent 能不能用。 | 合并为一张“已接入系统”卡片（上半表格与下半卡片用同一组件），标出启用路径；旧注册标“建议迁移”。；参照 §5.9 页面对照：系统接入 | W2 |
| SY2 | 系统接入 | P1 | 流程 | Grant to principal | 授权表单要手工粘贴 principal UUID，提示“The kernel has no principal directory to pick from yet”已过时（已有 list_principals，接入启动器里就有成员选择器），且只有英文。这正是 agent 所说的补救步骤。 | 换成成员选择器（复用启动器组件）；说明授权后入口 agent 能做什么。；参照 §5.9 原则 3 | W2 |
| SY3 | 系统接入 | P2 | 引导 | 页头 4 个入口 | 接入一个系统 / 申请连接 / 直接注册门 / 接入向导四个并列入口语义重叠；页面也没写“执行需要已发布的 Worker + 授权门”。 | 保留一个主入口（Launcher），其余进溢出菜单；加一段前提说明。；参照 §5.9 原则 1 | W2 |
| SY4 | 系统接入 | P3 | 细节 | 操作 chip / Publish manifest | 操作名截断“container.rest…”；“Publish manifest”看似禁用却无原因说明。 | chip 自适应宽度；禁用按钮给 tooltip。 | W2 |
| CO1 | 能力目录 · Operations | P2 | 数据 | Operation 描述 | 12 个 Operation 描述全空（行尾“· —”），也是 find_operations 自然语言检索落空的原因之一（B3）。 | manifest 导入时要求描述；页面允许 owner 补写。 | W2 |
| CO2 | 能力目录 · Operations | P2 | 治理配置 | document.parse | execute 类却标 auto-approvable，且没有影响级别。 | 核对 ragflow manifest；execute 默认不可自动批准，除非显式低影响。；参照 §5.9 原则 2 | W2 |
| CW1 | 能力目录 · Workers | P1 | 语义 / 安全 | Workers 标签 | 唯一一行是入口定义（v1 entry），还提供“弃用”——弃用它会让本工作区入口 agent 失效；也没有提示“本工作区 0 个可委派 Worker”或 ops-runner 模板入口。 | 入口定义单独展示且不可弃用（或高影响确认）；空态写清后果并给“从模板创建 ops-runner”。；参照 §5.9 原则 4 | W2 |
| CW2 | 能力目录 · Workers | P2 | 布局 | 行内操作 | 768 下“v1 entry”与“编辑（新版本草稿）”文字重叠。（需真窗复核） | 行内操作换行或收进菜单。 | W2 |
| CM1 | 能力目录 · Modules | P3 | 一致性 | 模块表 | 裸表格没有卡片容器，与其他列表不一致；ops-assets v1 可升级到 v2（未点）。 | 统一列表容器。 | W3 |
| GM1 | 模型与配额 | P2 | 语义 / 文案 | AgentPolicy | 可选模型“全不勾 = 不限制”是反向语义（取消最后一个勾反而放开）；“AgentPolicy”原始类型名作标题；表头与空态只有英文；侧栏“模型与配额”、页面“Models & Quotas”、平台“模型与供应商”三种叫法。 | 加显式“不限制”开关；标题改“工作区策略”。 | W3 |
| AU1 | 审计 | P2 | 溯源 | 溯源 / 重建 | 只收裸 id，没有从对象、事实一键进入的路径；资源类型筛选列原始 snake_case。 | 对象 / 关系筛选芯片 + ProvenanceChain（基线）；类型名本地化。；参照 §5.9 页面对照：审计 | W3 |
| O1 | 平台概览 | P1 | 基线差距 | 控制塔 | 缺基线的四指标（待处理 / 运行中 / 图谱新鲜度 / 费用）、“需要人处理”列表、验收残留横幅（现有 14 个残留工作区）。有“图谱新鲜度”指标的话，B1 会立刻可见。 | 按 §5.9 控制塔补齐。；参照 §5.9 页面对照：控制塔 | W2 |
| O2 | 平台概览 | P2 | 文案 | 开始使用清单 | 过期文案：“the runtime page lands in P-C”（运行层页已上线）；模型供应商“当前经主机配置”（S7-A 起可在控制台写）。 | 清单文案随功能状态更新。 | W3 |
| O3 | 平台概览 | P3 | 口径 | 计数卡 | 26 用户 / 15 工作区 / 18 门实例都含验收残留，与用户页（默认隐藏残留，3 人）、集成页（2 个实例）对不上。 | 计数与列表同口径，残留单独显示。 | W3 |
| PU1 | 用户 | P2 | 布局 | 用户表 | 1280 预算列“默认 default / 默认 default”折 4 行、表头折行；LOGIN 折行。 | 预算合成一列短文案；表头单语。 | W3 |
| PW1 | 工作区 | P1 | 数据呈现 | 生命周期列 | 13 个临时工作区全显示“到期 expires just now”，实际 expires_at 是 09-25 / 09-30（未到期）。 | 见 S9；显示“2 天后到期”。 | W1 |
| PW2 | 工作区 | P2 | 基线差距 | 残留筛选 | 默认不隐藏残留（基线要求默认隐藏；用户页已默认隐藏）：15 个里 14 个是 accept-*。 | 默认隐藏残留，与用户页一致。；参照 §5.9 页面对照：工作区 | W3 |
| PW3 | 工作区 | P2 | 布局 / 抽屉 | 配置抽屉 · 1280 表格 | 1280 表格溢出，“配置 Configure”被截；配置抽屉里 4 个独立“保存”；本体强制说明暴露 I-S5-1；“委托 owner”下拉列出验收残留用户。 | 抽屉一个保存；说明改用户语言；选择器排除残留用户。 | W3 |
| PI1 | 集成 | P1 | 确认 / 无障碍 | 接入包 · 模式列 | 行内下拉立即生效、无确认、显示原始枚举、6 个 select 没有标签。 | 见 S13；select 加 aria-label。；参照 §5.9 原则 4 | W3 |
| PI2 | 集成 | P2 | 语义漂移 | docker 接入包 | KIND 显示 http，而系统接入页同一个 docker 显示 cli。 | 统一 transport 的来源字段。 | W3 |
| PI3 | 集成 | P3 | 细节 | 门实例抽屉 | Gate id 截成“gatekeep”；“4 个工作区已启用”但不列出是哪 4 个；“BLAST RADIUS”、“启用它的工作区数”单语。 | 列出工作区（遗留 48 已记接口缺口）。；参照 遗留 48 | W3 |
| PM1 | 模块 | P3 | 确认 | 默认安装 | 复选框行内立即写入，无标签。 | 加标签与轻确认 / 撤销 Toast。；参照 §5.9 原则 4 | W3 |
| PMo1 | 模型与供应商 | P2 | 基线差距 | 供应商 / 矩阵 | 缺基线的“工作区 × 模型矩阵”（现只在单个工作区抽屉里配）；凭证 chip 中英重复过长。 | 补矩阵视图；凭证 chip 只写“环境变量 <供应商>_API_KEY”。；参照 §5.9 页面对照：模型与供应商 | W3 |
| PS1 | 平台设置 | P2 | id 暴露 | 默认工作区 | 默认工作区是 UUID 文本框；环境管理员暴露环境变量名；副标题只有英文。 | 改为工作区选择器；变量名放帮助 tooltip。；参照 §5.9 原则 3 | W3 |
| RT1 | 运行层 | P1 | 状态真实性 | 常驻容器 | 28 个已退出的入口容器全标“已是最新 Up to date”，没有运行 / 已退出列；约 7 行只剩已清除工作区的 UUID（孤儿），页面无清理入口。 | 加容器状态列；孤儿行标“工作区已清除”并提供清理。 | W4 |
| RT2 | 运行层 | P2 | 确认 | 回滚 | 确认卡渲染在页面最底部；文案含未渲染的 *different*；只有一个镜像时仍可“回滚”。 | 就近弹出；无上一镜像时禁用并说明。；参照 §5.9 原则 4 | W1 |
| RT3 | 运行层 | P3 | 溯源 | 活动镜像 / pi 漂移 | pi、extension、构建来源都是 dev；pi 漂移 unknown，详情暴露 /data/config/pi-drift.json 路径。 | 构建时写入版本；漂移见遗留 59。；参照 遗留 59 | W4 |
| ST1 | 运行状态 | P2 | 文案 | 备份卡 | 显示“未配置 Not configured — 未配置 not configured — 遗留 6 落地前如实显示；本能力不新增备份定时器”：内部遗留编号 + 重复文字。 | 改为“未配置备份 · 如何配置”链接。 | W3 |
| ST2 | 运行状态 | P3 | 细节 | 健康 / 用量 | egress-proxy 健康“未知”无解释；用量数字无千分位；健康 chip 在服务名前面。 | 说明未知原因；tabular-nums + 千分位；名称在前。 | W3 |
| PA1 | 平台审计 | P2 | 文案 / 一致性 | 平台审计 | 副标题说“每条平台级写入”，列表却几乎全是读；筛选“Action / Actor user id”只有英文且收裸 id；行布局与工作区审计不同。 | 见 S11；两个审计页用同一行组件。 | W3 |
| U2 | 系统接入 | P2 | 反馈 | Grant to principal 展开 | 表单原地插进卡片中部，没有滚动定位也没有高亮，长页面里几乎察觉不到。 | 改为抽屉（见 J6），或展开时滚动定位并临时高亮。 | W2 |
| U3 | 运行层 | P2 | 反馈 | 常驻容器 · 行首勾选框 | 每行都有勾选框，但没有任何批量操作条，勾了之后能做什么不交代。 | 勾选后浮出「已选 N · 停止 / 清理」操作条；功能未实现前先移除勾选框。；参照 RT1 | W3 |
| U4 | 我的智能体 | P3 | 识别而非记忆 | 继承工作区默认 | 三处「继承（不覆盖）」都看不到工作区默认值是什么，也没有链接过去。 | 旁边显示当前默认值摘要，并链到对应治理页。 | W3 |
| U5 | 系统接入 | P2 | 信息架构 | 页头 · 直接注册门 | 「直接注册门」是绕开门实例模型的旧路径（B7 的成因之一），却与推荐的「接入一个系统」同等视觉权重，没有「旧路径」标记。 | 收进溢出菜单并标「旧路径」；与 SY3 一并处理。；参照 B7 / SY3 | W2 |
| L1 | 平台概览 | P1 | 视觉层级 | 整页 | 版本、开始使用、四个计数、健康、约 18 行审计、绑定 API key 六种内容等重堆叠；审计占去一半纵向空间，比关键指标更显眼，「需要人处理」没有位置。 | 两段式：首屏放指标 + 健康 + 需要人处理；版本 / 开始使用 / 审计降为次区（审计最多 5 行）；绑定 API key 移出。；参照 §5.9 控制塔 | W3 |
| L4 | 系统接入 | P2 | 一致性 | 连接申请 | 「申请连接」在页头和空态里各出现一次。 | 只保留空态里的那个。 | W2 |
| L6 | 工作区 | P2 | 语义色 | 状态 / 用途 / Owners 列 | 三列都用色点 chip：健康状态、类型标签和身份归属用了同一种视觉语言，色点被稀释成装饰。 | Owners 用 RefChip 或纯文字；色点只给状态。；参照 §5.9 原则 2 | W3 |
| L7 | 运行层 | P2 | 冗余 | 活动镜像卡 | 「活动镜像」卡与「镜像清单」表的当前行重复同一组字段。 | 去掉卡片，在表格中高亮当前行。 | W3 |
| L9 | 审计 | P2 | 一致性 | 筛选区 | 同一页三种筛选栅格：溯源和重建是「大输入框 + 右侧按钮」，审计流是「四等宽控件 + 应用」。 | 统一成一个 FilterBar 组件。 | W3 |
| L10 | 我的智能体 | P2 | 一致性 | 当前生效 / 编辑表单 | 同一张卡里只读摘要用「标签在左」，可编辑表单用「标签在上」。 | 统一为标签在上。 | W3 |
| L12 | 平台概览 | P3 | 对齐 | 768 · 指标卡 | 「可用模型 Models available」标签多折一行，这张卡的数字比同排低一截。 | 标签区固定两行高，或缩短标签。 | W3 |
