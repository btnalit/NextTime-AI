{
  "name": "@nexttime/collector-host-inventory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Structural host-inventory collector (design doc §7.8, §10.1, §10.2; docs/development-tasks.md S3.3): dockerode + systemctl + git remote + a bounded process tree, sanitized before submission and written through the kernel's register_source/submit_observations capabilities.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dockerode": "^5.0.1"
  },
  "devDependencies": {
    "@types/dockerode": "^4.0.1"
  }
}
