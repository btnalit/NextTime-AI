{
  "name": "@nexttime/llm-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Per-provider passthrough proxy: verifies kernel-issued Handle signatures locally, injects real provider keys, whitelists models, reports usage back to the kernel (design doc §7.7).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  }
}
