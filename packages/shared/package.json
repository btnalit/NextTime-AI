{
  "name": "@nexttime/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Domain layer: enums, transition tables, capability registry, events and Zod schemas shared by every @nexttime/* package.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
