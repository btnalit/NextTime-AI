/**
 * adapters/gatekeeper-client: HTTP client implementing the gatekeeper protocol port
 * (describe_operations/observe/simulate/apply/revert/health — design doc §7.5).
 *
 * Placeholder for the R1 repo skeleton. Adapters may be imported only by application and
 * interfaces modules — enforced by .dependency-cruiser.cjs.
 */
export {};
