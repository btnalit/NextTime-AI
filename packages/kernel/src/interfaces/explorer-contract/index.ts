/**
 * interfaces/explorer-contract: Explorer Graph/Decision/Lineage endpoints (§9.5).
 *
 * Placeholder for the R1 repo skeleton (design doc §7.1, §7.10). Depends only on the
 * application and governance layers' service interfaces — never reaches into substrate
 * directly.
 */
export {};
