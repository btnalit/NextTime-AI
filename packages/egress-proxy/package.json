{
  "name": "@nexttime/egress-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Forwarding proxy on the control and workers networks: allows public egress, denies RFC1918/link-local/internal service names, resolves the source container to its WorkerRun or entry session and applies its allow/deny list, records per-domain byte counts (design doc §7.9).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
