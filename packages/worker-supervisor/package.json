{
  "name": "@nexttime/worker-supervisor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Docker-socket supervisor for entry and Worker containers: runsc, allowlisted nexttime/worker-runtime images only (design doc §7.3, §10.1).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  }
}
