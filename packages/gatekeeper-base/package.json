{
  "name": "@nexttime/gatekeeper-base",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gatekeeper protocol, four transport kinds (http/mcp/cli/ssh), manifest model, credential resolution, idempotent apply storage (design doc §7.5).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  }
}
